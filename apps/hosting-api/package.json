{
  "name": "@servitecperu/hosting-api",
  "version": "1.0.0",
  "private": true,
  "sideEffects": false,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "firebase deploy --only hosting:api"
  }
}
